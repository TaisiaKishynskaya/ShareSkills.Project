name: .NET Core CI

on:
  push:
    branches:
      - main
      - '**/feature/*'
  pull_request:
    branches:
      - main
      - '**/feature/*'

jobs:
  build-and-test:
    runs-on: windows-latest

    steps:
      # Checkout the code
      - name: Checkout code
        uses: actions/checkout@v3

      # Setup .NET Core SDK
      - name: Setup .NET Core SDK
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '7.x'

      # Install dependencies
      - name: Install dependencies
        run: dotnet workload restore

      # Restire NuGet packages
      - name: NuGet restore
        run: nuget restore ShareSkills.Project.sln

      # Build MobileClient
      - name: Build MobileClient
        run: dotnet build ./Client/MobileClient/MobileClient.csproj --no-restore --configuration Release -f net8.0-android

        # Build WebClient
      - name: Build WebClient
        run: dotnet build ./Client/WebClient/WebClient.csproj --no-restore --configuration Release -f net8.0

        # Build App
      - name: Build App
        run: dotnet build ./Server/App/App.csproj --no-restore --configuration Release

      # Run tests
      - name: Run tests
        run: dotnet test --no-build --verbosity normal
